# Chuẩn hóa ký tự xuống dòng
* text=auto

# Xử lý file nhị phân
*.jpg binary
*.png binary
*.gif binary
*.mp4 binary
*.zip binary
*.exe binary
*.dll binary

# Bỏ qua file log khi merge
*.log merge=drop
*.lock merge=drop

# Đảm bảo các file code luôn dùng LF
*.py text eol=lf
*.c text eol=lf
*.cpp text eol=lf
*.h text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf

# Loại bỏ file hệ thống của macOS
.DS_Store binary

# Windows script giữ CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Nếu sử dụng Git LFS (tùy chọn)
*.psd filter=lfs diff=lfs merge=lfs
*.mp4 filter=lfs diff=lfs merge=lfs
